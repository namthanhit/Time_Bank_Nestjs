export enum JobStatus {
    OPEN = "open",
    MATCHED = "matched",
    COMPLETED = "completed",
    CANCELLED = "cancelled",
    EXPIRED = "expired",
}

export enum JobVisibility {
    PUBLIC = "public",
    FRIENDS = "friends",
    HIDDEN = "hidden",
}